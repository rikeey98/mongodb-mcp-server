[project]
name = "mongodb-mcp"
version = "0.1.0"
description = "MongoDB MCP Server using FastMCP"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "pymongo",
]

[project.scripts]
mongodb-mcp = "server:mcp.run"